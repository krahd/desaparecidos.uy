# Documentation Status

The canonical project status report is [../STATUS.md](../STATUS.md).

This file intentionally remains a pointer so status information does not drift between `doc/` and the repository root.
