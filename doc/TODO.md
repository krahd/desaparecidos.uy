when crawling, crawl for people and places at the same time, mark them differently so that we use the correct images when creating the videos
move the target administration to its own page, so that the video generation interface does not collide with it
the buttons on the leftmost column (targets, manifests, review images, generate and outputs) they don't do anything, what should they do?
what are the sidecars?
let's move the crawling and images reviwing (places and faces) to its own page as well so that we can work on the images in one page, on the targets on another page and on the video generation on a third page.
